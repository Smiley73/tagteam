{
  "name": "tagteam-plugin",
  "version": "0.9.5",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
