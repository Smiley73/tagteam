{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "What the fixer did with each finding it was given",
  "description": "Bookkeeping, not proof. Every outcome here is re-checked by the reviewer that raised the finding.",
  "type": "object",
  "additionalProperties": false,
  "required": ["outcomes", "notes", "status", "summary", "unfinished"],
  "properties": {
    "outcomes": {
      "type": "array",
      "description": "Exactly one entry per finding supplied. A missing entry ends the pull request.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "outcome", "note"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "outcome": {
            "type": "string",
            "enum": ["fixed", "fixed-differently", "wont-fix", "failed"],
            "description": "fixed means the defect is gone by the repair the finding proposed, or by any repair where it proposed none; fixed-differently means the finding was right and the defect is gone, but the repair departs from the one the finding proposed. wont-fix means the finding is wrong or the repair is a decision for a person; failed means the repair was attempted and did not work."
          },
          "note": {
            "type": "string",
            "minLength": 1,
            "description": "What changed, or why nothing did."
          }
        }
      }
    },
    "notes": {
      "type": "string",
      "description": "Anything the reviewer re-checking this work needs and cannot read from the diff. An empty string when there is nothing; the key is still written."
    },
    "status": {
      "type": "string",
      "enum": ["complete", "unfinished"],
      "description": "complete only when you finished everything this dispatch handed you and `unfinished` is empty; unfinished otherwise. It is about the work you were given — a spec, a findings record, or a failing check — and never about anything you were not given: a fixer handed findings or a failing check answers for those and says nothing about a spec it never had. Claiming complete for work you did not finish is worse than saying unfinished, because the run stops for a person either way and only one of the two tells them the truth."
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "One or two sentences on what you changed, written for the person deciding whether this may merge."
    },
    "unfinished": {
      "type": "array",
      "description": "Every part of the work you were given and did not do, with the reason — including work you were told to do and could not: a file the spec names that does not exist, two statements that contradict, a repair that is a decision for a person. An empty array when nothing was left. A fixer reports here on the findings or the failing check it was handed, never on a spec it was not given.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["part", "reason"],
        "properties": {
          "part": {
            "type": "string",
            "minLength": 1,
            "description": "Which part of the work this is, named the way the thing you were given names it."
          },
          "reason": {
            "type": "string",
            "minLength": 1,
            "description": "Why it was left undone, in a sentence a person can act on."
          }
        }
      }
    }
  }
}
