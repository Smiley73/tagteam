{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "The front matter of one spec file",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "depends_on", "user_visible", "reviewers"],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[0-9]{2}-[a-z0-9][a-z0-9-]*$",
      "description": "Matches the file name without its extension. The numeric prefix is the default order."
    },
    "depends_on": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Spec ids that must be merged first. Empty for a spec that can start from the base branch."
    },
    "user_visible": {
      "type": "boolean",
      "description": "True when a person using the product would notice this change. True always waits for approval before merging."
    },
    "reviewers": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Lenses for this spec beyond the configured default set. A lens named here with a leading minus is removed from the default set for this spec."
    }
  }
}
