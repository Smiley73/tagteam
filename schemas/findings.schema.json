{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "One reviewer's findings on one candidate",
  "type": "object",
  "additionalProperties": false,
  "required": ["lens", "candidate", "summary", "findings"],
  "properties": {
    "lens": {
      "type": "string",
      "minLength": 1,
      "description": "The review lens this file answers for, e.g. security."
    },
    "candidate": {
      "type": "string",
      "pattern": "^[0-9a-f]{40,64}$",
      "description": "The commit reviewed. A findings file whose candidate is not the commit about to be merged is evidence about something else, and shipping refuses to count it."
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "One or two sentences on what was examined and what was concluded. Written even when nothing was found."
    },
    "findings": {
      "type": "array",
      "description": "Empty means the lens ran and found nothing. A missing file means the lens did not run, which is not the same thing.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["severity", "file", "line", "title", "detail", "fix"],
        "properties": {
          "severity": {
            "type": "string",
            "enum": ["blocking", "major", "minor", "nit"],
            "description": "blocking: must not merge. major: must not merge without a decision. minor and nit: recorded, never gating."
          },
          "file": {
            "type": ["string", "null"],
            "description": "Repository-relative path, or null when the finding is about the change as a whole."
          },
          "line": {
            "type": ["integer", "null"],
            "minimum": 1
          },
          "title": {
            "type": "string",
            "minLength": 1,
            "description": "One line naming the defect, not its category."
          },
          "detail": {
            "type": "string",
            "minLength": 1,
            "description": "Concrete inputs or state that produce the wrong behaviour. A finding that cannot name how it fails is a preference, and belongs at nit."
          },
          "fix": {
            "type": ["string", "null"],
            "description": "What to change, when that is obvious. Null when the right repair is a judgement call."
          }
        }
      }
    }
  }
}
