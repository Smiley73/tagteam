# Project conventions

Read the repository’s applicable `AGENTS.md`, `CLAUDE.md`, coding standards, lint configuration, and neighboring code. Where the request names this repository’s policy documents, those are the authoritative ones and are read first; discover the rest yourself. Check dependency direction, naming, framework idioms, package boundaries, test conventions, and generated-file policy. Project rules extend this rubric; cite the source rule for runtime-extension findings.
