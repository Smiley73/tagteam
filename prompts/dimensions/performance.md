# Performance

Check algorithmic complexity, repeated scans, N+1 access, sequential independent I/O, request/UI-thread blocking, unbounded buffering, redundant serialization, missing indexes, hot-path allocations, and cache invalidation. Tie every finding to a plausible scale and affected path.
