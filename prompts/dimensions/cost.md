# Cost

Look for regressions in algorithmic work, N+1 queries, chatty network or storage I/O, redundant model calls, unbounded tokens, oversized prompts, unnecessary high-tier models, wasteful cloud resources, missing batching/caching, and retry amplification. Quantify call or resource growth where countable.
