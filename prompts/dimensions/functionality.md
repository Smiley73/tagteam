# Functionality

Confirm the change meets its task and done criteria without regressions. Trace inputs to outputs, boundary conditions, branching, state transitions, ordering, off-by-one behavior, unreachable paths, and compatibility with callers. Verify every claimed behavior against executable code and tests.
