# Concurrency and data integrity

Check races, actor/thread isolation, cancellation, floating async work, locks and lock ordering, transaction scope, idempotency, check-then-act gaps, lost updates, uniqueness, migrations, schema compatibility, and concurrent retries. Follow shared state through all writers and readers.
