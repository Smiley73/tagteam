# Resiliency

Exercise adversarial and extreme inputs: oversized payloads, malformed encodings, resource exhaustion, burst load, degraded dependencies, restarts, and partial availability. State and caches must be bounded, owned, and recoverable.
