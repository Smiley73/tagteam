# Error handling

Trace failures from every boundary to the caller and user. Check swallowed exceptions, wrong fallback values, untyped errors where callers branch, incomplete cleanup, misleading messages, retryability classification, partial mutation, and whether diagnostics retain actionable context without leaking secrets.
