# Security

Trace untrusted input through validation, authorization, storage, logging, rendering, queries, commands, files, network calls, and model prompts. Check injection, access control, secret/PII exposure, traversal, SSRF, CSRF, unsafe deserialization, cryptography, least privilege, and prompt injection. Report a complete path and effective missing defense, not speculative hardening.
