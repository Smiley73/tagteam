# Code quality

Check clarity, cohesion, naming, duplication after the second repetition, unnecessary abstraction, hidden coupling, type-safety escapes, magic values, misplaced responsibilities, and whether the diff is the smallest coherent implementation. Flag structure only when it creates concrete maintenance or correctness risk.
