# Reliability

Check explicit timeouts, bounded and idempotent retry, fallback behavior, dependency outages, partial failures, durable state transitions, fail-safe decisions, and operational observability. A provider or model failure must not silently become success or safety.
