# Improve account lookup

1. Add focused query tests (simple).
2. Parameterize account lookup and handle database errors (medium; depends on 1).
3. Add a user-visible account status response and document it (complex; depends on 2).
